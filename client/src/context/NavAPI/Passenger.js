export class Passenger {
    constructor(shape, startNode, path=[]) {
        this.shape = shape
        this.startNode = startNode
        this.path
    }

    setPath(path) {
        this.path = path
    }
}

export default Passenger